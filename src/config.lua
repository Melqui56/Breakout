-- Config: shared game constants.
local Config = {
    width = 800,
    height = 600,

    ballRadius = 6,
    ballSpeed = 360,

    paddleWidth = 90,
    paddleHeight = 12,
    paddleSpeed = 420,
    paddleY = 560,
}

return Config
